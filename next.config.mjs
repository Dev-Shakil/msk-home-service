/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images:{
        domains:['media.graphassets.com','lh3.googleusercontent.com','media.licdn.com','avatars.githubusercontent.com']
    }
};

export default nextConfig;
